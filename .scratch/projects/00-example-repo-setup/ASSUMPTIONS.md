# Assumptions

- The target repo is `/home/andrew/Documents/Projects/remora-test`.
- `devenv` is available and should be used for project execution commands.
- `/home/andrew/Documents/Projects/remora-v2` exists and is the intended local bundle source.
- The setup objective is "ready to run" (validated config + successful local checks), not keeping a long-running runtime active in this session.
