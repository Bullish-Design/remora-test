# Assumptions

- The demo should be validated via CLI and HTTP endpoints without opening a browser GUI.
- Runtime should be bounded (`--run-seconds`) so the session is reproducible.
- Existing local model endpoint at `http://remora-server:8000/v1` remains reachable.
- `DEMO_REPORT.md` should be delivered at repository root for easy access.
