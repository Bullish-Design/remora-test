# Presenter Cue Sheet — Idea #6 Clone-to-Knowledge-Graph

1. Start from clean slate with `wipe`.
2. Run `setup` to clone the reference repository (`pallets-eco/blinker`) and initialize demo state.
3. Start runtime on `http://127.0.0.1:8081`.
4. Validate with `status`, `queries`, and filtered checks.
5. Capture UI proof with `check_ui_playwright`.
